rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write to all collections during development or configure per collection
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
